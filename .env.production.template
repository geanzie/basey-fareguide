# Production Environment Variables Template
# Copy this file and configure with your actual production values
# DO NOT commit files with real secrets to version control!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string (use Neon, Supabase, or your own)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Base URL for your application
NEXTAUTH_URL="https://your-domain.com"

# NextAuth.js secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="GENERATE_RANDOM_SECRET_HERE"

# JWT Secret for token signing - Generate with: openssl rand -hex 32
JWT_SECRET="GENERATE_RANDOM_JWT_SECRET_HERE"

# ============================================
# GOOGLE MAPS API
# ============================================
# Client-side API key (restricted to your domain)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your_google_maps_client_api_key"

# Server-side API key (restricted to server IPs)
GOOGLE_MAPS_SERVER_API_KEY="your_google_maps_server_api_key"

# ============================================
# EMAIL SERVICE (RESEND)
# ============================================
# Get your API key from https://resend.com
RESEND_API_KEY="re_your_resend_api_key"

# Email sender address (must be verified in Resend)
EMAIL_FROM="Basey Fare Guide <noreply@your-domain.com>"

# ============================================
# ERROR MONITORING (SENTRY)
# ============================================
# Get your DSN from https://sentry.io
SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Public Sentry DSN for client-side monitoring
NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# Sentry auth token for sourcemap uploads (optional)
SENTRY_AUTH_TOKEN="your_sentry_auth_token"

# Sentry organization and project
SENTRY_ORG="your-organization"
SENTRY_PROJECT="your-project"

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NODE_ENV="production"

# Enable bundle analyzer (set to 'true' to analyze bundle size)
ANALYZE="false"

# ============================================
# RATE LIMITING (Optional - for Redis/Upstash)
# ============================================
# If using Redis for distributed rate limiting
# REDIS_URL="redis://username:password@host:port"
# UPSTASH_REDIS_REST_URL="https://your-upstash-url.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.production.template .env.production
# 2. Fill in all the values with your actual credentials
# 3. NEVER commit .env.production to git
# 4. For Vercel deployment, add these as environment variables in the dashboard
# 5. Generate secure secrets using:
#    - openssl rand -base64 32  (for NEXTAUTH_SECRET)
#    - openssl rand -hex 32     (for JWT_SECRET)
