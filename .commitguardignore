# Silence only the entropy heuristic for the test suite. Import paths and
# fixture identifiers there read as high-entropy strings; every hard rule
# (private keys, provider tokens, .env files, database dumps) still applies.
entropy:src/__tests__/**
